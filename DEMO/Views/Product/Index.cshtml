@model List<DEMO.Models.Product>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <h2 class="py-3">SẢN PHẨM TÌM ĐƯỢC</h2>
    <hr>
    <div class="row">
        <div class="container-fluid bg-trasparent my-4 p-3" style="position: relative">
            <div class="row row-cols-1 row-cols-xs-2 row-cols-sm-2 row-cols-lg-4 g-3">
                @foreach (var item in Model)
                {
                    <div class="col hp">
                        <div class="card h-100 shadow-sm">
                            <a style="text-decoration:none" target="_blank" href="@Url.Action("Details", "Product", new { id = item.product_id })">
                                <img src=@Url.Content("~/Assets/Admin/image/" + item.product_image) class="card-img-top" alt="product.title" />
                            </a>

                            <div class="card-body">
                                <div class="clearfix mb-3">
                                    <span class="float-start badge rounded-pill bg-success">@(item.product_price.ToString("N0") ?? "")đ</span>
                                    <span class="float-end">@item.product_quantity</span>
                                </div>
                                <h5 class="card-title">
                                    <p>@item.product_name</p>
                                </h5>
                                @using (Html.BeginForm("AddCart", "ShoppingCart", new { id = item.product_id }, FormMethod.Post))
                                {
                                    <div class="d-grid gap-2 my-4">
                                        <button type="submit" class="btn btn-warning bold-btn">Add to cart</button>
                                    </div>
                                }

                                <div class="clearfix mb-1">

                                    <span class="float-start"><a href="#"><i class="fas fa-question-circle"></i></a></span>

                                    <span class="float-end">

                                        <i class="far fa-heart" style="cursor: pointer"></i>

                                    </span>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

